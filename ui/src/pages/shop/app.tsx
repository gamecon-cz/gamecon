import { FunctionComponent, render } from 'preact'

type TShopProps = {

};

export const Shop: FunctionComponent<TShopProps> = (props) => {
  const {} = props;

  return <>
    
  </>;
};

Shop.displayName = "Shop";
